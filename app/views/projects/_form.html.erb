<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div class="alert alert-danger" id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<form accept-charset="UTF-8" action="/projects" class="form-horizontal" id="new_project" method="post">
  <div style="display:none">
    <input name="utf8" type="hidden" value="✓">
    <input name="authenticity_token" type="hidden" value="/ZXnBdI5HPZqOlRMuiaFlCYFC93JnKC2zadASSP4lNs=">
  </div>

  <div class="form-group row">
    <%= f.label :name, :class => "control-label col-md-2" %>
    <div class="form-controls col-md-4">
      <%= f.text_field :name, :class => 'form-control', :id => "name" %>
    </div>
  </div>

  <div class="form-group row">
    <div class="col-md-offset-2 col-md-4">
      <%= f.submit :class => "btn btn-primary" %>
      <a class='btn btn-default' href="/projects">Cancel</a>
    </div>
  </div>

</form>

<% end %>
