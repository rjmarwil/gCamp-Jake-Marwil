<%= form_for(@task) do |f| %>
  <% if @task.errors.any? %>
    <div class="alert alert-danger" id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<form accept-charset="UTF-8" action="/tasks" class="form-horizontal" id="new_task" method="post">
  <div style="display:none">
    <input name="utf8" type="hidden" value="✓">
    <input name="authenticity_token" type="hidden" value="/ZXnBdI5HPZqOlRMuiaFlCYFC93JnKC2zadASSP4lNs=">
  </div>

  <div class="form-group row">
    <%= f.label :description, :class => "control-label col-md-2" %>
    <div class="form-controls col-md-4">
      <%= f.text_field :description, :class => 'form-control', :id => "task_description" %>
    </div>
  </div>

  <div class="form-group row">
    <%= f.label :due_date, :class => "control-label col-md-2" %>
    <div class="form-controls col-md-4">
      <%= f.date_select :due_date, :class => 'form-control', :id => 'due_date' %>
    </div>
  </div>

  <div class="form-group row">
    <% unless @task.new_record? %>
      <div class="control-label col-md-2">
        <label>Completed?</label>
      </div>
      <div class="col-md-4">
       <% if @task.complete == true %>
       <%= f.check_box :complete, { :checked => 'checked'} %>
       <% else %>
       <%= f.check_box :complete %>
       <% end %>
      </div>
    <% end %>
  </div>

  <div class="form-group row">
    <div class="col-md-offset-2 col-md-4">
      <%= f.submit :class => "btn btn-primary" %>
      <a class='btn btn-default' href="/tasks">Cancel</a>
    </div>
  </div>

</form>

<% end %>
